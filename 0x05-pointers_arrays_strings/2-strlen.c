#include "main.h"
#include <string.h>
/**
* _strlen - takes a pointer as a parameter and returns the length
* @s: it is the string
* Return: returns length of the string
*/
int _strlen(char *s)
{
return (strlen(s));
}
